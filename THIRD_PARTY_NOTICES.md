# Third-party notices

See `go.mod`, `go.sum`, and `NOTICE` for dependency and license notices.

The service uses the MIT-licensed maintained forks of `quic-go` and
`connect-ip-go` pinned in `go.mod`. The application does not include the former
CONNECT-UDP/TCP relay or TUN offload/GRO adaptations.
