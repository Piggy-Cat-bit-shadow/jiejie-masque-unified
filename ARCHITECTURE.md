# Architecture

One Go module and binary. CONNECT-IP remains an independent engine with TUN, session NAT, mTLS, watchdog, and host-network behavior. CONNECT-UDP is a separate engine with its own configuration and service.
